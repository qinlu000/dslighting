diff --git a/datacope/general/pyproject.toml b/datacope/general/pyproject.toml
index 110f859..6c6f452 100644
--- a/datacope/general/pyproject.toml
+++ b/datacope/general/pyproject.toml
@@ -34,7 +34,7 @@ dependencies = [
     "docker",
     "jupyter-client",
     "claude-agent-sdk==0.2.108",
-    "openai-codex-sdk==0.1.11"
+    "openai-codex==0.1.0b3"
 ]
 
 [tool.setuptools.packages.find]
diff --git a/datacope/general/src/runtimes/agents/codex_agent.py b/datacope/general/src/runtimes/agents/codex_agent.py
index 49e2fb4..42bdfdd 100644
--- a/datacope/general/src/runtimes/agents/codex_agent.py
+++ b/datacope/general/src/runtimes/agents/codex_agent.py
@@ -1,205 +1,262 @@
+import json
 import os
+import shutil
+import tempfile
 import time
-import asyncio
 import traceback
-import json
-from typing import Dict, Any
+from pathlib import Path
+from typing import Any, Dict
 
+from src.core.schema import AgentResult
 from src.runtimes.base_agent import BaseAgent
 from src.runtimes.registry import register_agent
-from src.core.schema import AgentResult
+
 
 AGENT_TYPE = "codex"
 
+
 @register_agent(AGENT_TYPE)
 class CodexAgent(BaseAgent):
-    """Autonomous data science agent using OpenAI Codex SDK.
+    """Autonomous skill author backed by the Codex SDK and selected local CLI.
 
-    Codex is a full agent with built-in bash/file tools. Instead of the manual
-    backend + environment loop used by ReActDSAgent, this agent sends the task
-    to Codex and lets it run autonomously.
+    Codex reuses the user's existing ChatGPT login. Each run gets a disposable
+    authoring directory containing only the skill-creator instructions, while
+    the requested skill output directory is added as the sole extra writable
+    root.
     """
 
     def __init__(self, backend: str, model: str, **kwargs):
         super().__init__(AGENT_TYPE, model, backend, **kwargs)
 
-        self.working_dir = kwargs.get('working_dir', '.')
-        self.output_schema = kwargs.get('output_schema', {})
-        self.api_key = kwargs.get('api_key') or os.environ.get('OPENAI_API_KEY', '')
-        self.base_url = kwargs.get('base_url') or os.environ.get('OPENAI_API_BASE', 'https://api.openai.com/v1')
-        self.timeout = kwargs.get('timeout', None)
-
-    async def _run_codex(self, prompt: str, working_directory: str = ".") -> Dict[str, Any]:
-        """Run a single Codex autonomous session and return the result."""
-        from openai_codex_sdk import Codex
-
-        codex = Codex({"api_key": self.api_key, "base_url": self.base_url})
-
-        thread_opts: Dict[str, Any] = {
-            "working_directory": working_directory,
-        }
-        if self.model:
-            thread_opts["model"] = self.model
-
-        thread = codex.start_thread(thread_opts)
-
-        run_opts: Dict[str, Any] = {}
-        if self.output_schema:
-            run_opts["output_schema"] = self._make_openai_strict_schema(self.output_schema)
+        self.working_dir = Path(kwargs.get("working_dir", ".")).expanduser().resolve()
+        self.output_schema = kwargs.get("output_schema", {})
+        codex_bin = kwargs.get("codex_bin")
+        if not codex_bin:
+            raise ValueError("CodexAgent requires codex_bin")
+        self.codex_bin = Path(codex_bin).expanduser().resolve()
+        if not self.codex_bin.is_file():
+            raise ValueError(f"codex_bin is not a file: {self.codex_bin}")
+
+        skill_output_dir = kwargs.get("skill_output_dir")
+        if not skill_output_dir:
+            raise ValueError("CodexAgent requires skill_output_dir")
+        self.skill_output_dir = Path(skill_output_dir).expanduser().resolve()
+        self.skill_output_dir.mkdir(parents=True, exist_ok=True)
+
+        default_skill_creator = Path(__file__).resolve().parents[3] / "skills" / "skill-creator"
+        self.skill_creator_dir = Path(
+            kwargs.get("skill_creator_dir", default_skill_creator)
+        ).expanduser().resolve()
+        if not self.skill_creator_dir.is_dir():
+            raise ValueError(
+                f"skill_creator_dir is not a directory: {self.skill_creator_dir}"
+            )
 
-        try:
-            turn = await thread.run(prompt, run_opts)
-        except Exception as e:
-            raise RuntimeError(f"Codex SDK error: {e}")
-
-        return {
-            "final_response": getattr(turn, "final_response", "") or "",
-            "id": getattr(turn, "id", ""),
-            "thread_id": getattr(turn, "thread_id", ""),
-            "items": getattr(turn, "items", []),
-        }
-
-    def _run_codex_sync(self, prompt: str, working_directory: str = ".") -> Dict[str, Any]:
-        async def runner():
-            if self.timeout is None:
-                return await self._run_codex(prompt, working_directory)
-            return await asyncio.wait_for(
-                self._run_codex(prompt, working_directory),
-                timeout=self.timeout,
+    def _copy_skill_creator(self, authoring_dir: Path) -> None:
+        """Copy trusted authoring guidance without carrying symlinks across."""
+        for path in self.skill_creator_dir.rglob("*"):
+            if path.is_symlink():
+                raise ValueError(f"skill_creator_dir must not contain symlinks: {path}")
+
+        target = authoring_dir / "skills" / "skill-creator"
+        target.parent.mkdir(parents=True, exist_ok=True)
+        shutil.copytree(self.skill_creator_dir, target)
+
+    def _codex_config(self, authoring_dir: Path, codex_home: Path):
+        from openai_codex import CodexConfig
+
+        writable_root = json.dumps(str(self.skill_output_dir))
+        return CodexConfig(
+            codex_bin=str(self.codex_bin),
+            cwd=str(authoring_dir),
+            env={"CODEX_HOME": str(codex_home)},
+            config_overrides=(
+                f"sandbox_workspace_write.writable_roots=[{writable_root}]",
+                "sandbox_workspace_write.network_access=false",
+                "sandbox_workspace_write.exclude_slash_tmp=true",
+                "sandbox_workspace_write.exclude_tmpdir_env_var=true",
+                'web_search="disabled"',
+                "tools.web_search=false",
+                'model_reasoning_effort="xhigh"',
+                'shell_environment_policy.inherit="core"',
+            ),
+        )
+
+    @staticmethod
+    def _copy_chatgpt_auth(isolated_home: Path) -> None:
+        """Isolate Codex from user config/MCPs while reusing its saved login."""
+        source_home = Path(
+            os.environ.get("CODEX_HOME", Path.home() / ".codex")
+        ).expanduser().resolve()
+        source_auth = source_home / "auth.json"
+        if source_auth.is_symlink() or not source_auth.is_file():
+            raise RuntimeError(
+                "A file-backed Codex ChatGPT login is required; run `codex login` first"
             )
-        
-        try:
-            loop = asyncio.get_running_loop()
-        except RuntimeError:
-            loop = None
 
-        if loop and loop.is_running():
-            import concurrent.futures
-            outer_timeout = None if self.timeout is None else self.timeout + 30
-            with concurrent.futures.ThreadPoolExecutor() as pool:
-                future = pool.submit(lambda: asyncio.run(runner()))
-                return future.result(timeout=outer_timeout)
+        isolated_home.chmod(0o700)
+        target_auth = isolated_home / "auth.json"
+        shutil.copy2(source_auth, target_auth)
+        target_auth.chmod(0o600)
+
+    def _run_codex(self, prompt: str, system: str) -> Dict[str, Any]:
+        """Run one isolated Codex session using the existing ChatGPT login."""
+        from openai_codex import ApprovalMode, Codex, Sandbox
+
+        with (
+            tempfile.TemporaryDirectory(prefix="datacope-codex-") as tmp_dir,
+            tempfile.TemporaryDirectory(prefix="datacope-codex-home-") as home_dir,
+        ):
+            authoring_dir = Path(tmp_dir).resolve()
+            codex_home = Path(home_dir).resolve()
+            self._copy_skill_creator(authoring_dir)
+            self._copy_chatgpt_auth(codex_home)
+
+            with Codex(self._codex_config(authoring_dir, codex_home)) as codex:
+                account_response = codex.account()
+                account = getattr(account_response.account, "root", account_response.account)
+                if account is None or getattr(account, "type", None) != "chatgpt":
+                    raise RuntimeError(
+                        "Codex requires an existing ChatGPT login; run `codex login` first"
+                    )
+
+                thread = codex.thread_start(
+                    approval_mode=ApprovalMode.deny_all,
+                    cwd=str(authoring_dir),
+                    developer_instructions=system or None,
+                    ephemeral=True,
+                    model=self.model or None,
+                    sandbox=Sandbox.workspace_write,
+                )
+                turn = thread.run(
+                    prompt,
+                    output_schema=(
+                        self._make_openai_strict_schema(self.output_schema)
+                        if self.output_schema
+                        else None
+                    ),
+                )
+
+                items = [self._serialize_model(item) for item in turn.items]
+                return {
+                    "final_response": turn.final_response or "",
+                    "id": turn.id,
+                    "thread_id": thread.id,
+                    "items": items,
+                    "usage": self._serialize_model(turn.usage),
+                    "status": getattr(turn.status, "value", str(turn.status)),
+                }
 
-        return asyncio.run(runner())
+    @staticmethod
+    def _serialize_model(value: Any) -> Any:
+        if value is None:
+            return None
+        if hasattr(value, "model_dump"):
+            return value.model_dump(mode="json", by_alias=True)
+        if isinstance(value, (str, int, float, bool, list, dict)):
+            return value
+        return str(value)
 
     # ------------------------------------------------------------------
     # BaseAgent interface
     # ------------------------------------------------------------------
 
     def solve_task(self, prompt: str, system: str = "", **kwargs) -> AgentResult:
+        del kwargs
         start_time = time.time()
         actual_turns = 0
-        env = None
 
         try:
-            result = self._run_codex_sync(f"{system}\n\n{prompt}" if system else prompt, self.working_dir)
+            result = self._run_codex(prompt, system)
             items = result.get("items", [])
-            
-            agent_conversation = []
-            for item in items:
-                if item.type == "command_execution":
-                    actual_turns += 1
-                    agent_conversation.append({
-                        "type": "command_execution",
-                        "command": item.command,
-                        "aggregated_output": item.aggregated_output,
-                    })
-                elif item.type == "agent_message":
-                    agent_conversation.append({
-                        "type": "agent_message",
-                        "text": item.text,
-                    })
-                else:
-                    agent_conversation.append({
-                        "type": item.type,
-                        "text": getattr(item, "text", "Cannot extract text"),
-                    })
+            actual_turns = sum(
+                item.get("type") == "commandExecution"
+                for item in items
+                if isinstance(item, dict)
+            )
 
             response_text = result["final_response"]
-
             raw_conversation = [
-                {
-                    "type": "system_message",
-                    "text": system,
-                },
-                {
-                    "type": "user_message",
-                    "text": prompt,
-                }
-            ] + agent_conversation
-
+                {"type": "system_message", "text": system},
+                {"type": "user_message", "text": prompt},
+                *items,
+            ]
             execution_time = time.time() - start_time
-            
-            results_fields = {
-                'conversation': raw_conversation,
-                'response': self._parse_results(response_text) if response_text else {},
-                'raw_response': response_text,
-                'turns': actual_turns,
-                'error': None,
-                'metadata': {
-                    'model': self.model,
-                    'agent_type': 'codex',
-                    'execution_time': execution_time,
-                    'conversation_length': len(raw_conversation),
-                    'codex_turn_id': result.get("id", ""),
-                    'codex_thread_id': result.get("thread_id", ""),
+
+            return AgentResult(
+                conversation=raw_conversation,
+                response=self._parse_results(response_text) if response_text else {},
+                raw_response=response_text,
+                turns=actual_turns,
+                error=None,
+                metadata={
+                    "model": self.model,
+                    "agent_type": AGENT_TYPE,
+                    "auth_type": "chatgpt",
+                    "execution_time": execution_time,
+                    "conversation_length": len(raw_conversation),
+                    "codex_turn_id": result.get("id", ""),
+                    "codex_thread_id": result.get("thread_id", ""),
+                    "token_usage": result.get("usage"),
+                    "status": result.get("status", ""),
                 },
-                'raw_result': {
-                    'raw_conversation': raw_conversation,
+                raw_result={
+                    "raw_conversation": raw_conversation,
                     "raw_response": response_text,
-                    'turns': actual_turns,
-                    'codex_items': result.get("items", []),
+                    "turns": actual_turns,
+                    "codex_items": items,
                 },
-            }
-
-            return AgentResult(**results_fields)
+            )
 
-        except Exception as e:
+        except Exception as exc:
             execution_time = time.time() - start_time
             error_trace = traceback.format_exc()
-            print(f"CodexAgent error: {e}")
+            print(f"CodexAgent error: {exc}")
             print(f"CodexAgent error trace: {error_trace}")
-            results_fields = {
-                'conversation': [],
-                'response': '',
-                'raw_response': '',
-                'turns': actual_turns,
-                'error': str(e),
-                'metadata': {
-                    'model': self.model,
-                    'agent_type': 'codex',
-                    'execution_time': execution_time,
-                    'error_trace': error_trace,
+            return AgentResult(
+                conversation=[],
+                response={},
+                raw_response="",
+                turns=actual_turns,
+                error=str(exc),
+                metadata={
+                    "model": self.model,
+                    "agent_type": AGENT_TYPE,
+                    "execution_time": execution_time,
+                    "error_trace": error_trace,
                 },
-                'raw_result': None,
-            }
-
-            return AgentResult(**results_fields)
-        finally:
-            if env is not None:
-                env.close()
+                raw_result={},
+            )
 
     def _make_openai_strict_schema(self, schema: dict[str, Any]) -> dict[str, Any]:
-        """Convert a Pydantic JSON schema to OpenAI strict structured output format.
-
-        OpenAI's Responses API requires:
-            - "additionalProperties": false at the top level
-            - "required" must list ALL property keys (no optional fields)
-
-        Pydantic's model_json_schema() only puts truly required fields in "required",
-        but OpenAI demands every property is listed. Fields with defaults still work
-        because the model will always produce them.
-        """
-        strict = {**schema, "additionalProperties": False}
-        if "properties" in strict:
-            strict["required"] = list(strict["properties"].keys())
+        """Recursively make object schemas valid for strict structured output."""
+        strict: dict[str, Any] = {}
+        for key, value in schema.items():
+            if isinstance(value, dict):
+                strict[key] = self._make_openai_strict_schema(value)
+            elif isinstance(value, list):
+                strict[key] = [
+                    self._make_openai_strict_schema(item)
+                    if isinstance(item, dict)
+                    else item
+                    for item in value
+                ]
+            else:
+                strict[key] = value
+
+        properties = strict.get("properties")
+        if isinstance(properties, dict):
+            strict["additionalProperties"] = False
+            strict["required"] = list(properties)
         return strict
 
-    def _parse_results(self, response_text: str) -> Dict[str, Any]:
-        """Parse the raw result from Codex SDK into a structured format."""
-        if response_text:
-            try:
-                parsed = json.loads(response_text)
-            except json.JSONDecodeError as e:
-                raise ValueError(f"Failed to parse Codex response as JSON: {e}")
-        return parsed
\ No newline at end of file
+    @staticmethod
+    def _parse_results(response_text: str) -> Dict[str, Any]:
+        """Parse the structured Codex response."""
+        try:
+            parsed = json.loads(response_text)
+        except json.JSONDecodeError as exc:
+            raise ValueError(f"Failed to parse Codex response as JSON: {exc}") from exc
+        if not isinstance(parsed, dict):
+            raise ValueError("Codex structured response must be a JSON object")
+        return parsed
